// User Template For C++

class Solution {
  public:
    int countgroup(vector<int>& arr) {
        int res=0;
        for(int i=0;i<arr.size();i++)
        {
            res=res^arr[i];
        }
        if(res==0)
        {
            return arr.size();
        }
        return 0;
    }
};

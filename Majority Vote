// User Template For C++

class Solution {
  public:
    vector<int> findMajority(vector<int>& nums) {
        unordered_map<int, int> mp;
        for(int i:nums){
            mp[i]++;
        }
        vector<int> ans;
        for(auto i:nums){
            if(mp[i]>nums.size()/3){
                ans.push_back(i);
                mp[i]=0;
            }
        }
        if(ans.size()==0){
            ans.push_back(-1);
        }
        return ans;
    }
};

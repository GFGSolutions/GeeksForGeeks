// User Template For C++

class Solution {
  public:
    int subarraySum(vector<int>& arr) {
        int sum = 0,n = arr.size();
        for(int i = 0;i<n;i++){
            sum+=(i+1)*(n-i)*arr[i];
        }
        return sum;
    }
};

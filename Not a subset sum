// User Template For C++

class Solution {
  public:
    long long findSmallest(vector<int> &arr) {
        long long res = 1;
        for(auto it : arr)
        {
            if(it>res)
            break;
            res += it;
        }
        return res;
    }
};
